#pragma once

#include <QWidget>

class SimulatorControl : public QWidget
{
    Q_OBJECT

public:
    SimulatorControl( QWidget* parent = nullptr );


private:
    //const grid_map* grid = nullptr;
    //std::vector<grid_cell> path;

};
